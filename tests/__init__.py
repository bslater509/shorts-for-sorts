"""Test suite for Shorts for Sorts.

Organised by module under test:
    test_subtitles      — ASS subtitle generation and emoji matching.
    test_emoji          — Emoji map loading and matching.
    test_batch          — Batch job config, progress console, progress bar.
    test_progress_utils — Progress percentage calculation, elapsed time formatting.
    test_llm_utils      — Title/hashtag parsing and retry-with-backoff.
    test_models         — Pydantic request/response model validation.
    test_routers        — API route handlers (health, settings, batch prompts).
    test_ws_manager     — WebSocket connection manager and client notification.
"""
